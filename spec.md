# ⚡ CAGED 有氧吉他特訓核心系統 — 產品需求規格書 (PRD)

## 📌 1. 產品核心價值與情境痛點
本系統專為吉他演奏者在進行**有氧運動（如走坡度機、跑步機）**時，提供高強度「視覺 × 聽覺 × 幾何直覺」的樂理指板反應訓練。

* **抗震動盲點**：全螢幕高對比 AMOLED 純黑底色搭配超飽和霓虹燈號，即使身體劇烈晃動，也能以餘光捕捉節拍、和弦與指板位置。
* **免除手動負擔**：進入訓練後，系統會依 Web Audio API 排程自動輪轉和弦、刷弦、音階與預告節拍；目前實作尚未使用 Screen Wake Lock API，因此「自動防休眠」不列為已實作功能，螢幕常亮需依使用者裝置、PWA 或瀏覽器設定處理。
* **高階調式避坑**：使用者自訂音程代號時，系統後台根據當前和弦資料自動完成「調式映射（Mode Mapping）」，降低在不同級數間手動換算升降音的負擔。
* **本機保存**：設定會保存於瀏覽器 `localStorage`，重新開啟同一網域時可復原主調、和弦進行、BPM、Stage、左右手模式與自訂音序設定。

---

## 🛠️ 2. 介面佈局與功能要件

### 2.1 初始設定頁面（Setup Screen）
1. **左手模式切換**：提供一鍵水平翻轉吉他指板功能。翻轉時只調整琴格與空弦欄的排列方向，文字本身不做鏡像翻轉；右手模式顯示「琴頭向左」，左手模式顯示「琴頭向右」。
2. **全螢幕顯示**：提供一鍵切換瀏覽器全螢幕（Fullscreen API / WebKit Fullscreen API，由使用者手勢觸發）。對於不支援的行動瀏覽器，透過 `apple-mobile-web-app-capable`、`mobile-web-app-capable`、`viewport-fit=cover`、`theme-color` 等 meta 標籤提供「加入主畫面」與沉浸顯示後備方案。
3. **主調 Key 選擇器**：以網格顯示 12 個半音按鈕（`C` 到 `B`），決定訓練的母調根音。
4. **和弦進行組裝器（清單優先流程）**：
   * 進行以「和弦清單」為核心，**一律先加入清單再使用**：訓練永遠使用「和弦清單中選中的那筆」，建立 / 編輯用的 builder 只是暫時子頁，其內容不會直接被播放。
   * 清單檢視以「和弦清單 / 管理」兩個分頁切換（無獨立的「自由自訂」分頁）：
     * **和弦清單**：以按鈕格顯示一份**統一管理**的進行清單（內建與自訂等價）。選取樣式與其他選單一致——**選中＝綠底、未選＝灰底**；點卡片即選取。格子末端有一張**「＋ 新增進行」虛線仮卡片**，點它切到 builder 子頁建立新進行。
     * **管理**：每張卡底部顯示大顆的「編輯」「刪除」按鈕（無圖示、寬版、文字灰、hover 時編輯轉綠 / 刪除轉紅）。綠色高亮只套用在上方的進行名稱區；管理按鈕列固定為暗底，即使該卡為選中狀態也不會變綠。管理分頁不顯示「＋ 新增進行」卡。
   * **清單為 seed 統一管理**：內建預設（`I - IV - V`、`ii - V - I`、`vi - IV - I - V`、`I - vi - ii - V`）只作為**第一次載入**的 seed 來源，seed 進清單後全部項目（含內建）都可**選取、編輯、刪除**。內建 seed 項目使用穩定 id `preset:<名稱>`，使用者新增項目使用隨機 id。
   * 系統第一次開啟或沒有保存設定時，清單為四組內建預設，預設選用 `I - IV - V`。
   * **builder 子頁（新增 / 編輯）**：由「＋ 新增進行」或管理分頁的「編輯」進入，以「拖拉字卡」UI 組裝進行，並提供「保存」「取消」：
     * **保存**：把 builder 內容存為新的一筆（顯示名稱**純自動產生**，由級數以 ` - ` 相連，如 `I - IV - vi`）或就地更新正在編輯的那筆，然後回到和弦清單並選中該筆。空進行時「保存」為停用。
     * **加入時去重**：新增時若清單已有級數完全相同的進行，**不重複新增**，直接回清單並選中既有那筆。
     * **取消**：破棄變更、回到和弦清單；編輯既有筆時原資料保持不變（僅在保存時才寫入）。
     * builder 開啟期間，最下方「進入有氧特訓 START」按鈕會停用，促使先保存或取消。
     * 刪除某筆時：若刪的正是目前選中，選取自動跳到剩餘第一筆或回退預設；若刪的正是編輯中的那筆，會結束編輯狀態。**清單被刪空時無法開始訓練**：最下方 START 按鈕停用並顯示「請先新增一組和弦進行」。
   * builder 可用和弦庫依性質分為七大類：
     * **順階三和弦**：`I, ii, iii, IV, V, vi, vii°`
     * **順階七和弦**：`IM7, iim7, iiim7, IVM7, V7, vim7, viim7b5`
     * **同主調三和弦（Modal Interchange）**：`i, ii°, bIII, iv, v, bVI, bVII`
     * **同主調七和弦**：`im7, iim7b5, bIIIM7, ivm7, vm7, bVIM7, bVII7`
     * **副屬七和弦（Secondary Dominant）**：`V7/ii, V7/iii, V7/IV, V7/V, V7/vi`
     * **關聯 II（Related II）**：`IIm7b5/ii, IIm7b5/iii, IIm7/IV, IIm7/V, IIm7b5/vi`
     * **裏和弦（SubV7 / Tritone Substitute）**：`SubV7/I, SubV7/ii, SubV7/iii, SubV7/IV, SubV7/V, SubV7/vi`
   * 使用者可透過拖曳或直接點擊卡片將和弦加入進行列。
   * 拖曳以 **Pointer Events** 統一實作，桌機滑鼠與手機觸控皆可拖曳；拖曳中顯示跟隨指針的浮動字卡，未達拖曳門檻則視為點擊（庫卡新增 / 進行卡移除）。
   * 支援拖曳插入特定縫隙、重排特定順位與動畫擠壓效果。拖曳重排放開後，被移動的字卡會換新 id 並以原地淡入動畫呈現，避免從版面左上角飛入的破圖。
   * builder 空進行時「保存」停用；清單被刪空時無法開始訓練（START 停用），避免空進行造成播放錯誤。
   * **底層資料模型**：每張和弦字卡以統一結構描述（`offset` 根音半音距離、`mode` 調式音階、`family` 大小調 / 屬七 / 減和弦體系、`label` 和弦性質、`modeName` 調式名稱）。新增字卡主要需於 `CHORD_MODES` 增加資料，指板、音訊、爬音邏輯即可共用。
5. **BPM 調整欄**：提供一條 range 滑桿（`step=5`，每格 5 BPM），旁邊即時顯示目前數值；可調範圍 40–240 BPM，預設 85 BPM。讀取保存設定時也會將 BPM 限制在此範圍。
6. **三驅動音序模式**（三選一的「音程特訓模式演算法」引擎：`stage` 階梯爬升 / `custom` 自訂音序器 / `triad` 三和弦琶音）：
   * **階梯爬升解鎖（`stage`）**：可切換兩種獨立的「音程特訓模式演算法」，各自有自己的 Stage 數與解鎖曲線；`CAGED_SCALES` 由 7 種調式 × 5 種 CAGED 型（共 35 個音階型）以演算法生成，其 `path` 決定實際播放順序，Stage 只負責依音程過濾。設定保存時會各自記住目前選用的模式與 Stage。
     * **和弦基礎模式（`chord`，預設，Stage 1-5）**：Triad → 7th → 9th → 11th → 13th，逐階為三和弦疊加和弦延伸色彩。
       * **Major / Dominant 體系**：
         * Stage 1（三和弦）：`1, 3, 5`
         * Stage 2（三和弦 + 7th）：`1, 3, 5, 7, b7`
         * Stage 3（三和弦 + 9th）：`1, 3, 5, 2, b2`
         * Stage 4（三和弦 + 11th）：`1, 3, 5, 4, #4`
         * Stage 5（三和弦 + 13th）：`1, 3, 5, 6, b6`
       * **Minor 體系**：
         * Stage 1（三和弦）：`1, b3, 5`
         * Stage 2（三和弦 + 7th）：`1, b3, 5, b7, 7`
         * Stage 3（三和弦 + 9th）：`1, b3, 5, 2, b2`
         * Stage 4（三和弦 + 11th）：`1, b3, 5, 4, #4`
         * Stage 5（三和弦 + 13th）：`1, b3, 5, 6, b6`
       * **Diminished 體系**：
         * Stage 1（三和弦）：`1, b3, b5`
         * Stage 2（三和弦 + 7th）：`1, b3, b5, b7, 7`
         * Stage 3（三和弦 + 9th）：`1, b3, b5, 2, b2`
         * Stage 4（三和弦 + 11th）：`1, b3, b5, 4, #4`
         * Stage 5（三和弦 + 13th）：`1, b3, b5, 6, b6`
       * 每個 Stage 列出的候選音程（如 `7` 與 `b7`、`2` 與 `b2`、`4` 與 `#4`、`6` 與 `b6`）會再依當前調式實際存在的音程過濾，例如 Ionian 只留 `7`、Mixolydian 只留 `b7`、Lydian 只留 `#4`。
     * **音階基礎模式（`scale`，Stage 1-3）**：Triad → Pentatonic → Mode Scale。
       * **Major 體系**：Stage 1（大小三和弦）`1, 3, 5` → Stage 2（大小五聲音階）`1, 2, 3, 5, 6` → Stage 3（調式音階）解鎖完整 Mode Scale
       * **Minor 體系**：Stage 1 `1, b3, 5` → Stage 2 `1, b3, 4, 5, b7` → Stage 3 解鎖完整 Mode Scale
       * **Diminished 體系**：Stage 1 `1, b3, b5` → Stage 2 `1, b3, 4, b5, b7` → Stage 3 解鎖完整 Mode Scale
     * 兩種模式的 Stage 過濾後，若同一條弦、同一個 offset 連續出現，會自動去除重複音，避免折返點重複播放同一音。
   * **自訂音序器（Custom Sequence Builder，`custom`）**：文字輸入框接受逗號分隔的相對音程指令，如 `L5, L6, 1, 2, L7, 1`。目前實作支援 `1`–`7` 與大寫 `L` 前綴，`L` 代表低八度。系統會依目前和弦的 `mode` 陣列將度數映射為絕對音高。
   * **三和弦琶音（Triad Practice，`triad`）**：在選定的「連續三弦組」上，用三和弦彈奏上方的和弦進行；不使用 CAGED 音階型，而是為整組進行即時計算三和弦轉回形。
     * **可選弦組**：`1-3`、`2-4`、`3-5`、`4-6`（吉他弦號，`1` 為最細弦）。四選一，`TRIAD_STRING_SETS` 定義各組低音弦→高音弦的排列。
     * **轉回形自動決定（Voice Leading）**：對進行中每個和弦，依和弦性質（Major/Dominant 取 `1,3,5`、Minor 取 `1,b3,5`、Diminished 取 `1,b3,b5`）算出三個構成音，於選定三弦上枚舉三個轉回形（低音為 root / 3rd / 5th）與不同八度落點，再以貪婪法選出「與前一個和弦同弦位琴格移動量總和最小」的轉回形。結果是整輪進行的三和弦集中在同一把位、只切換轉回形（位置相近），而非到處跳。
     * **避免連續同轉回形**：計分時對「與前一個和弦相同的轉回形」加上柔性懲罰（`SAME_INVERSION_PENALTY`，目前為 4，以琴格移動量為單位），盡量不要在進行中連續使用同一個轉回形。這是「盡量」而非硬性限制：若同轉回形的把位明顯更近（差距大於懲罰值，例如同一個和弦連續出現時），仍會沿用同轉回形。一般不同和弦的進行下，相鄰和弦幾乎都會落在不同轉回形。
     * **排除誇張跨度**：枚舉候選時會先排除琴格跨度過大（超過 `MAX_TRIAD_SPAN`，目前為 5 格）的 voicing，避免出現「低音壓開放弦、高音卻在第 9~10 格」這類實際按不出來的極端指型，改用其它轉回形；若某和弦所有候選都過大（極少見）才退回原始候選。
     * **把位階梯推進**：每一輪進行的錨點（第一個和弦）會依把位由低到高排成階梯，第 `cycle` 輪取第 `cycle` 個（循環）；因此每完成一輪整組把位就往高處推進，與 CAGED 自動循環的體驗一致。
     * **爬音方向（上升 / 下降）**：可在模式選擇區另外選擇 Train 的爬音方向（`selectedTriadDirection`：`ascending` 低音→高音，預設 / `descending` 高音→低音）。方向只影響 Train 三個構成音的播放與點亮順序（依 `pitchScore` 昇冪或降冪），不影響轉回形挑選與 Prep 刷弦。此設定會保存於 `localStorage`。
     * **播放行為**：Prep 刷選定三弦的三和弦（低音弦→高音弦），且會**隨進行中每個和弦即時切換**；Train 依所選方向（上升 低→高 / 下降 高→低）逐拍彈奏三個構成音；Predict 提前導航至下一輪（更高把位）的三和弦把位。此模式沿用和弦進行（內建或自訂）。爬音與刷弦皆由音訊引擎依 `currentChordIdx` / `currentCagedCycle` 即時計算，與排程器同步。
7. **把位自動循環（Dynamic CAGED Auto-Cycling）**：
   * 系統依和弦根音計算 C / A / G / E / D 五種 CAGED 型的可用把位區間，只保留位於 0–15 格範圍內且根音不超過 14 格的型。
   * 可用型會依把位中心點由低到高排序；第 0 輪會選擇最接近琴首低把位（中心約第 2 格）的型。
   * 每完成一整輪和弦進行，CAGED cycle 增加 1，下一輪會使用排序後的下一個可用高把位；不是固定以 `C → A → G → E → D` 的字面順序循環。
8. **設定保存與復原**：
   * 保存項目包含：主調、目前選取的進行 id（`selectedProgressionName`，內建為 `preset:<名稱>`、自訂為隨機 id）、統一進行清單（`savedProgressions`：每筆 `{ id, name, chords }`，含 seed 內建與使用者新增）、BPM、音程特訓模式（`chord` / `scale`）、Stage、左右手模式、指板音程顯示基準（`chord` / `key`）、音序引擎（`trainingInputMode`：`stage` / `custom` / `triad`）、三和弦弦組（`selectedTriadStringSet`）、三和弦爬音方向（`selectedTriadDirection`：`ascending` / `descending`）、自訂音序文字。builder 的暫時編輯狀態（是否開啟、編輯緩衝）**不保存**，重整後一律回到和弦清單。
   * `savedProgressions` 會逐筆過濾（`chords` 必須非空且皆為存在的級數，缺 `name` / `id` 會自動補上）；即使過濾後為空也會尊重（代表使用者把清單刪光）。`selectedProgressionName` 找不到對應 id 時，會回退預設 `preset:I - IV - V` 或清單剩餘第一筆。若保存資料格式錯誤或包含不存在的和弦，系統會過濾或回退預設值，不中斷訓練功能；Stage 上限也會依保存的音程特訓模式（`chord`=5 / `scale`=3）重新夾範圍。
   * **後方相容**：舊版只有 `userProgressions`（自訂進行集）而無 `savedProgressions` 欄位時，會自動遷移為「seed 內建預設 ＋ 併入舊 `userProgressions`」；舊的 `selectedProgressionName`（內建名稱）會映射為 `preset:<名稱>`。
   * **後方相容**：舊版只保存 `isCustomSequenceMode`（布林）而無 `trainingInputMode` 欄位時，`true` 會遷移為 `custom`、`false` 遷移為 `stage`；未知的三弦組值會回退為預設 `2-4`。

### 2.2 核心訓練頁面（Training Screen）— 運動極簡佈局
1. **上方：當前和弦與指示區**：
   * 超巨大字體顯示當前級數（如 `vi`）。下方顯示絕對根音與和弦性質（如 `A m 和弦時間`）以及目前 Mode 名稱。
   * 常駐 4 點 LED 節拍指示燈，第 1 拍以紅色突出，其餘拍以琥珀色提示。
   * 當前和弦永遠水平置中；非預告期時隨節拍呼吸（奇數拍縮小、其餘維持正常大小），預告期維持固定尺寸。
   * 所選和弦進行列會顯示完整進行（如 `I › IV › V`），並即時高亮目前所在的和弦。
   * **預告期（Predict）指示**：當進入一般預告期時，在當前和弦正中央的右側顯示 `→ 下一個和弦`，下一個和弦以黑 / 紅雙色閃爍；閃爍週期與 BPM 同步（每拍一次完整循環）。導入預告期不顯示箭頭與下一個和弦。
2. **下方：動態吉他指板**：
   * 橫向顯示 6 弦與 0–15 格（含空弦欄），共 16 欄。
   * 琴格寬度依十二平均律比例遞減，空弦欄使用較窄固定比例，讓指板寬度更接近真實吉他。
   * 指板會依可用寬高自動等比縮放，維持固定長寬比；手機橫向或高度不足時，訓練畫面會壓縮上方資訊區並保留指板完整顯示，不使用橫向捲動。
   * **全時幾何基礎**：只有主調根音位置以灰色正方形常駐顯示；不顯示 CDEFGAB 等音名文字。其他音平時不常駐顯示。
   * **Prep 階段灰色和弦提示**：準備期除了主調根音灰色正方形外，會額外以灰色圓點 / 方點顯示當前 CAGED 和弦 voicing，並標示該和弦音程（如 `1, 3, 5, b7`）。這些灰色提示也會與 Prep 第一拍的刷弦資料共用。
   * **和弦根音點亮邏輯**：和弦根音不常駐亮紅；只有在 Train 階段播放到根音時才以紅色霓虹效果亮起。
   * **音階把位區間標示**：虛線 + 半透明長方形目前採用「整組和弦進行」在同一 CAGED cycle 內的最小～最大琴格作為統一把位邊界，而不是每個和弦各自畫一個邊界。若預告期的下一個和弦會回到進行第 0 項，代表下一輪即將開始，指板會提前切到下一個 CAGED cycle 的整組邊界。
   * **三和弦模式的把位**：三和弦琶音模式（`triad`）以「整組和弦進行」在同一輪（`cycle`）內所有三和弦的最小～最大琴格上下各留 1 格，畫成綠色虛線矩形框住整輪；且矩形在**弦方向只涵蓋所選的連續三弦**（`scaleRegionOverride` 額外帶入 `minStringIndex` / `maxStringIndex`），不再跨滿 6 弦。每完成一輪把位往高處階梯推進，預告期若下一個和弦回到第 0 項會提前切到下一輪（更高）的邊界。訓練畫面「目前把位」標籤改為顯示所選弦組（如 `2-4 弦三和弦`）。
   * **訓練期閃爍與文字**：Train 階段播放到的音會以音程顏色閃爍並顯示音程文字：根音=紅、三 / 五度與 `b5`=藍、七度=紫、其餘調式音=綠。
   * **自訂音序顯示**：自訂模式依 token 計算絕對音高後，會在指板上點亮所有相同 pitch class 的位置，並額外加上琥珀色外圈；這與預設 CAGED path 的「單一弦格位置」不同。
   * **弦粗細差異**：1 弦（最高音）最細，6 弦（最低音）最粗，視覺上呈現真實吉他弦的漸變粗細。
   * **指板標示點（Inlays）**：在第 3、5、7、9、12、15 格標示圓形標記點，其中第 12 格為雙點，其餘為單點。
   * **弦間無分隔線**：弦與弦之間不顯示水平分隔線，只保留各弦本身的金屬線條與垂直琴格線。
3. **底部 / 控制列**：
   * 訓練畫面提供較小的「退出特訓」按鈕與目前把位文字提示（如 `目前把位：E 型`）。
   * **音程顯示基準切換**：提供「從 Chord」／「從 Key」二選一按鈕，即時切換指板音程顯示基準（`intervalDisplayMode`）：從 Chord 時音程數字相對於目前和弦根音（例如 V 和弦上 G 顯示 `1`）；從 Key 時音程數字相對於主調 Key 根音（例如 C Key 裡 G 顯示 `5`）。此設定會保存於 `localStorage`。
   * **Key 根音定位點常駐顯示開關**：訓練畫面提供「■ Key 根音定位 ON / OFF」切換鈕（`showKeyRootMarkers`）。ON（預設）時主調根音以灰色正方形常駐顯示為定位基準；OFF 時這些正方形不再常駐，只在實際播放到（Train）或 Prep 和弦指型提示裡才出現，用來驗證是否真的背下把位。此設定會保存於 `localStorage`。
   * 目前實作沒有訓練畫面內的超大暫停 / 恢復鍵；暫停邏輯存在於音訊引擎 `toggle()`，但 UI 只有進入訓練前的開始按鈕與訓練中的退出按鈕。
4. **安全區與不捲動版面**：
   * 訓練畫面使用 `100dvh`、`overflow: hidden` 與 iOS safe-area padding，目標是在 PC、iPad 橫屏與 iPhone 橫屏中不需上下捲動即可看到主要資訊。

---

## ⏱️ 3. 動態時序三部曲（4/4 拍律動演算法）

系統對每一個和弦維持一個**動態長度**的大循環，時序邏輯如下：

| 階段名稱 | 拍數區間 | 聽覺行為（Audio） | 視覺行為（Visual） |
| :--- | :--- | :--- | :--- |
| **導入預告期（Intro Predict）** | 訓練開始前 4 拍 | 只播放 587.33Hz 高音 Click；不播放刷弦，也不播放英語人聲。 | 第一個和弦置中並以黑 / 紅雙色閃爍；不顯示 `→ 下一個和弦`；不計入一輪和弦進行，因此不觸發 CAGED cycle 推進。 |
| **準備期（Prep）** | 前 4 拍 | 每拍播放 Click，第 1 拍 880Hz、其餘拍 440Hz；第 1 拍依資料定義式 CAGED voicing 由粗弦到細弦刷弦，每弦約錯開 0.025 秒。 | 顯示主調根音灰色正方形，並額外顯示當前 CAGED 和弦 voicing 的灰色音程提示。 |
| **訓練期（Train）** | N 拍 | 每 1 拍播放一個音階單音；此階段不疊加節拍器 Click。預設模式依靜態 CAGED scale path 與 Stage 過濾結果播放；自訂模式依 token 循環播放。彈完 N 顆音後立即切換至預告期，不補齊 4 拍倍數。 | 預設模式只點亮 path 中當前發聲的單一弦格；自訂模式點亮相同 pitch class 的所有位置。發音音符以音程色彩閃爍並顯示音程文字。 |
| **預告期（Predict）** | 最後 4 拍 | 單音演奏停止，改為每拍播放 587.33Hz 高音 Click；目前沒有英語人聲 `One, Two, Three, Four`。 | 當前和弦維持置中且固定尺寸，右側顯示 `→ 下一個和弦`，下一個和弦以 BPM 同步的黑 / 紅閃爍預告。若即將回到進行第 0 項，指板的統一把位邊界提前切換到下一個 CAGED cycle。 |

### 3.1 音訊排程與合成音
* 音訊引擎使用 Web Audio API，排程器以 25ms lookahead 週期運作，並提前約 0.1 秒排程下一批音符。
* 撥弦音色以兩個微 detune oscillator、泛音週期波、音量包絡、低通濾波、短 delay feedback、dry / wet 混合與極輕微 LFO 組合而成。
* 停止播放時會清除排程 timer，並呼叫 `speechSynthesis.cancel()` 作為保險；但目前沒有實際排程語音播報。

---

## 🧠 4. 核心樂理技術：調式自動映射機制

### 4.1 和弦資料模型
`CHORD_MODES` 是和弦、音階與顯示共用的核心資料來源。每個和弦包含：

* `offset`：和弦根音相對於主調 Key 的半音距離。
* `mode`：相對於和弦根音的調式音階半音陣列，用於自訂音序映射。
* `family`：`major`、`minor`、`dim` 或 `dom`，用於 Stage 取音策略。
* `label`：顯示與 voicing library 對應用的和弦性質。
* `modeName`：顯示與 CAGED scale mode 判斷用的調式名稱。

### 4.2 自訂音序的 Mode Mapping
常駐顯示的灰色正方形為該 Key 的根音定位點；實際發聲的音階音只在播放到時依音程色彩亮起。當自訂輸入 `L5, L6, 1, 2, L7, 1` 時，系統根據當前和弦的 `mode` 陣列將每個度數轉成絕對音高：

* **I 級和弦（以 C 為例）** → Ionian：`1, 2, 3, 4, 5, 6, 7`
  * token 對應音：`G → A → C → D → B → C`
* **ii 級和弦（以 Dm 為例）** → Dorian：`1, 2, b3, 4, 5, 6, b7`
  * token `7` 會映射到該 mode 的第 7 音，也就是實際的 `b7`；對應音：`A → B → D → E → C → D`
* **vi 級和弦（以 Am 為例）** → Aeolian：`1, 2, b3, 4, 5, b6, b7`
  * token `6`、`7` 會映射到該 mode 的 `b6`、`b7`；對應音：`E → F → A → B → G → A`

### 4.3 CAGED scale 與 diminished 的現況
* CAGED scale 資料由演算法生成：手寫的只有各 CAGED 型的幾何（`CAGED_FORM_GEOMETRY`：`rootString` 與各弦的 offset 視窗），`buildCagedScaleShape()` 依「調式 × 幾何」推算每個音的弦 / 品格 / 音程，並生成 `path`（根音 → 最高音 → 最低音 → 回根音）。
* 生成時強制「同音異弦零」：同一個絕對音只會落在一條弦上（三全音 `#4` 靠細弦、`b5` 靠粗弦）。需要手動指定某型的練習順序時，可在 `PATH_OVERRIDES` 覆寫該型的 `path`。
* 7 種 church modes × 5 種 CAGED forms 共 35 個 shape 會在開發模式中檢查完整性（含音高與音程一致、同音異弦零的檢算）。
* **7th 和弦 voicing 每個 CAGED 型只有單一按法**：`maj7` / `m7` / `7`（dom7）/ `m7♭5` 依指板圖，每個 form（C/A/G/E/D）固定一個 voicing，已移除 `omit5` / `shell` 等替代按法。因此 `resolveCagedChordVoicing()` 在未指定 option 時回傳的就是該型唯一的 voicing，chord 與 scale 使用同一個 form，不會再出現「G 型 scale 卻顯示 E 型和弦按法」的情形。三和弦 `maj` / `min` / `dim` 與 `dim7` 維持原狀。
* 目前和弦庫沒有提供 `vii°7`（減七和弦）字卡，`vii°` 只以減三和弦（`dim`）形式存在。
* `dim`（減三和弦，R/♭3/♭5）與 `dim7`（減七和弦，R/♭3/♭5/6＝♭♭7）各自擁有 5 個 CAGED 型的單一按法；`dim7` 已不再沿用 `dim` 的按法（先前的 alias 已解除）。`dim7` 的第 4 音因音程表無 `♭♭7`，以相同音高（9 半音）的 `6` 表示。目前和弦庫沒有 `dim7` 字卡（`vii°` 仍為 `dim`），故 `dim7` 按法尚未被任何字卡使用，屬預先備妥。

---

## 🧾 5. 與目前實作對齊後的規格修正摘要

* 預設和弦進行修正為 `I - IV - V`。
* 移除「已實作自動防休眠」描述；目前只有全螢幕 / PWA meta 後備方案，沒有 Screen Wake Lock API。
* 預告期與導入預告期修正為高音 Click，沒有英語人聲倒數。
* Prep 視覺修正為會顯示灰色 CAGED 和弦 voicing 提示，不是只顯示 Key 根音方塊。
* 指板把位框修正為整組和弦進行的統一 min–max 邊界，不是每個和弦各自的邊界。
* CAGED cycle 修正為依可用把位中心點排序後向高把位推進，不是固定 `C → A → G → E → D` 字面順序。
* Stage 規則補上 `dom` 視為 major、`dim` 專用 Stage、Stage 4 的實際音程清單與調式過濾邏輯。
* 補充 `localStorage` 設定保存、演算法生成的 35 個 CAGED scale shape、資料定義式 CAGED voicing、訓練畫面無暫停 / 恢復按鈕等實作現況。
* 和弦庫由三大類修正為七大類，補上同主調三和弦 / 同主調七和弦 / 副屬七和弦 / 關聯 II / 裏和弦（SubV7）。
* 階梯解鎖規則修正為 `chord`（Stage 1-5：Triad → 7th / 9th / 11th / 13th）與 `scale`（Stage 1-3：Triad → Pentatonic → Mode Scale）雙模式，取代原本單一 Stage 1-5 描述；Stage 上限依模式各自為 5 與 3。
* 補充訓練畫面「音程顯示基準」（從 Chord / 從 Key）切換功能，並列入 `localStorage` 保存項目。
* 新增訓練畫面「Key 根音定位點常駐顯示」ON / OFF 切換（`showKeyRootMarkers`），並列入 `localStorage` 保存項目。
* 移除 `vii°7` 已實作的錯誤描述；目前和弦庫沒有此字卡，`dim7` 僅為預留的 label / voicing，尚未被任何和弦使用。
* 新增第三種音序引擎「三和弦琶音」（`triad`）：在選定的連續三弦組（`1-3` / `2-4` / `3-5` / `4-6`）上，以最小琴格移動（Voice Leading）自動選出轉回形彈奏和弦進行。音序引擎切換由原本的布林 `isCustomSequenceMode` 改為三選一的 `trainingInputMode`（`stage` / `custom` / `triad`），並保存 `selectedTriadStringSet`，同時提供舊設定的後方相容遷移。
* CAGED scale shape 由手寫改為演算法生成（僅手寫各 form 幾何 `CAGED_FORM_GEOMETRY`），並保證同音異弦為零。
* 7th 和弦（`maj7` / `m7` / `7` / `m7♭5`）的 CAGED voicing 統一為「每個 form 單一按法」（依指板圖），移除 `omit5` / `shell` 等替代 voicing；同時修正 `maj7` / `m7` / `m7♭5` 的 G 型被誤寫成 E 型指型的資料錯誤。
* `dim` / `dim7` 也改為每個 CAGED 型單一按法（`dim7` 依指板圖、`dim` 由 `dim7` 去掉 6th 推得），並解除 `dim7 = dim` 的 alias 使兩者成為獨立資料。
* 三和弦琶音的轉回形挑選加入「盡量不連續使用同一轉回形」的柔性懲罰（`SAME_INVERSION_PENALTY`）。
* 三和弦琶音新增「爬音方向」選項（`selectedTriadDirection`：上升 `ascending` / 下降 `descending`），並列入 `localStorage` 保存項目。
* 和弦進行改為「seed 統一清單」（`savedProgressions`）：內建預設只作為第一次載入的 seed，之後全部（含內建）都可選取、編輯、刪除。選取樣式統一為選中＝綠、未選＝灰（與主調 Key 選擇一致）。`selectedProgressionName` 語意為「進行 id」（內建 `preset:<名稱>` 或自訂隨機 id），並提供舊 `userProgressions` 設定的自動遷移。
* 和弦進行改為**清單優先流程**：分頁精簡為「和弦清單 / 管理」，移除「自由自訂」分頁；builder 改為經「＋ 新增進行」仮卡片（新增）或管理分頁「編輯」進入的**子頁**，提供「保存 / 取消」。訓練一律使用清單選中項（`getActiveProgression` 不再讀 builder 內容），builder 開啟期間停用 START。保存新進行時會**去重**（已有相同級數則直接選中既有那筆）。builder 的暫時狀態（`isCustomProgMode` / `customProgressionArray`）不再保存於 `localStorage`。
* BPM 調整由 `+` / `-` 按鈕改為 range 滑桿（`step=5`，40–240），旁顯示數值；只保留滑桿。
