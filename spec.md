# ⚡ CAGED 有氧吉他特訓核心系統 — 產品需求規格書 (PRD)

## 📌 1. 產品核心價值與情境痛點
本系統專為吉他演奏者在進行**有氧運動（如走坡度機、跑步機）**時，提供高強度「視覺 ✕ 聽覺 ✕ 幾何直覺」的樂理指板反應訓練。
* **抗震動盲點**：全螢幕高對比 Amoled 純黑底色搭配超飽和霓虹燈號，即使身體劇烈晃動，也能以餘光精準捕捉。
* **免除手動負擔**：上機後手機自動防休眠，結合 Web Audio API 硬體級時鐘，和弦與音階自動無限輪轉。
* **高階調式避坑**：使用者自訂音程代號時，系統後台根據當前順階級數自動完成「調式映射（Diatonic Mode Mapping）」，確保不跑調且精準解決。

---

## 🛠️ 2. 介面佈局與功能要件

### 2.1 初始設定頁面 (Setup Screen)
1. **左手模式切換**：提供一鍵水平翻轉吉他指板功能。翻轉時僅調整琴格物理排列順序（如：12琴格至空弦），內嵌音名文字維持正常不翻轉，確保左撇子吉他手閱讀體驗。
2. **全螢幕顯示**：提供一鍵切換瀏覽器全螢幕（Fullscreen API，由使用者手勢觸發）；對於不支援的行動瀏覽器（如 iOS Safari），透過 `apple-mobile-web-app-capable` 等 meta 標籤提供「加入主畫面」全螢幕後備方案。App 並依瀏覽器尺寸自動縮放，確保所有元件不被遮擋。
2. **主調 Key 選擇器**：網格化 12 個半音按鈕（C 到 B），決定訓練的母調基礎。
3. **和弦進行組裝器 (Drag-and-Drop Chords)**：
   * 支援內建預設：`I - IV - V`、`ii - V - I`、`vi - IV - I - V`、`I - vi - ii - V`（系統預設選用 `vi - IV - I - V`）。
   * 自由自訂模式改為「拖拉字卡」UI。**可用和弦庫依性質分為三大類**，方便擴充與搭配使用：
     * **順階三和弦**：`I, ii, iii, IV, V, vi, vii°`
     * **順階七和弦**：`IM7, iim7, iiim7, IVM7, V7, vim7, viim7b5`
     * **調外 / 借用和弦 (Modal Interchange)**：`bIII, bVI, bVII, vii°7`
   * 使用者可透過拖曳或直接點擊卡片將和弦加入進行列。
   * 拖曳以 **Pointer Events 統一實作**，桌機滑鼠與手機觸控皆可拖曳；拖曳中顯示跟隨指針的浮動字卡，未達拖曳門檻則視為點擊（庫卡新增 / 進行卡移除）。
   * 支援**拖曳重排特定順位**與**動畫擠壓效果**的直覺操作。拖曳重排放開後，被移動的字卡以原地淡入動畫呈現（避免從版面左上角飛入的破圖）；拖曳經過其他字卡時，下方字卡不會誤觸發紅色移除提示。
   * **底層資料模型**：每張和弦字卡以統一結構描述（`offset` 根音半音距離、`mode` 調式音階、`family` 大小調體系、`label` 和弦性質、`modeName` 調式名稱），新增字卡僅需於 `CHORD_MODES` 增加一筆，指板 / 音訊 / 爬音三大引擎即自動支援。
4. **盲點 BPM 調整欄**：超大 `+` / `-` 按鈕（每次 ±5 BPM），防止運動中手指晃動誤觸；可調範圍 40–240 BPM，預設 85 BPM。
5. **雙驅動音序模式**：
   * **階梯解鎖模式 (Stage 1-5)**：動態產生把位內音階，並區分大/小調體系自動映射。
     * **Stage 1 (三和弦)**：大調(1, 3, 5) / 小調(1, \flat3, 5)
     * **Stage 2 (增加音)**：大調(+2度) / 小調(+4度)
     * **Stage 3 (五聲音階)**：大調(1, 2, 3, 5, 6) / 小調(1, \flat3, 4, 5, \flat7)
     * **Stage 4 (七度色彩)**：五聲 + 大/小七度
     * **Stage 5 (自然音階)**：解鎖該把位調式全自然音
   * **自訂音序器 (Custom Sequence Builder)**：文字輸入框，接受相對音程指令（如 `L5, L6, 1, 2, L7, 1`），`L` 代表低音八度。
7. **把位自動循環 (Dynamic CAGED Auto-Cycling)**：系統為每一個和弦動態尋找最符合的 CAGED 型態。一開始會優先尋找靠近琴首的最低把位（例如 C Key 中，4 級會用 E 型，6m 級會用 Am 型）。並且每一次和弦進行循環結束後，下一個循環所有的指型都會自動往高把位（`C -> A -> G -> E -> D` 的順序）橫移推進。

### 2.2 核心訓練頁面 (Training Screen) - 運動極簡佈局
1. **上腹部：當前和弦與指示區**：
   * 超巨大字體顯示當前級數（如 **vi**）與絕對和弦名稱（如 Am）。常駐 4 點 LED 節拍指示燈。
   * **當前和弦永遠水平置中**；非預告期時隨節拍呼吸（奇數拍縮小、其餘維持正常大小），預告期則維持固定尺寸。
   * **所選和弦進行列**：在指示區顯示完整的和弦進行（如 `I › IV › V`），並即時高亮目前所在的和弦。
   * **預告期 (Predict) 指示**：取消舊有的「頂部和弦倒數預告區」。改為在當前和弦正中央緊鄰右側，顯示「**→ 下一個和弦**」，且下一個和弦進行**高頻率黑 / 紅雙色閃爍**，閃爍週期**與 BPM 同步**（每拍一次完整黑紅循環）。
2. **下腹部：動態吉他指板**：
   * 橫向拉伸，6 弦垂直，共 15 格琴格（外加空弦欄）。
   * **自適應縮放**：指板會依瀏覽器寬度自動等比縮放（不再橫向捲動），確保在手機等小螢幕上所有琴格與元件皆完整可見、不被裁切。
   * **全時幾何基礎**：僅該 Key 的音恆為**常駐灰色正方形**，作為全指板幾何定位基準，**不顯示任何 CDEFGAB 等音名文字**。其餘調內音與和弦根音**平時不常駐顯示**，非調內音完全隱形。
   * **和弦根音點亮邏輯**：和弦根音（音程 1）**不再常駐亮紅**。僅在 Train 階段播放到該音時，才以**紅色**（含霓虹發光效果）點亮，平時保持隱形。
   * **音階把位區間標示**：當前和弦行進所需的音階琴格範圍，以**虛線 + 半透明的長方形**框出（取該 CAGED 把位所有弦的最小～最大琴格，呈齊整矩形而非鋸齒凹凸），讓使用者一眼鎖定該在哪個格區間找音。
   * **訓練期閃爍與文字**：Train 階段根據所選 Stage 模式，當前播放的音程以色彩美學閃爍顯示（根音=紅、三五度=藍、七度=紫、其餘=綠），**同時會在該發光音符上標示對應的音程數字（如 $1, 3, 5, \flat3$ 等）**。
   * **弦粗細差異**：1 弦（最高音）最細，6 弦（最低音）最粗，視覺上呈現真實吉他弦的漸變粗細。
   * **指板標示點 (Inlays)**：在第 3、5、7、9、12、15 格標示圓形標記點。其中第 12 格為雙點（上下各一），其餘格為單點。
   * **弦間無分隔線**：弦與弦之間不顯示水平分隔線，僅保留各弦本身的金屬線條。
   * **把位動態邊界**：系統在底層依據 CAGED 動態推算邊界以限制音階的顯示，視覺上取消了舊有的翡翠綠色發光框，提供更乾淨的指板。
3. **底部：安全防滑操作區**：包含超大暫停/恢復鍵、退出特訓鍵，以及目前把位型態的文字提示。

---

## ⏱️ 3. 動態時序三部曲（4/4 拍律動演算法）

系統對每一個和弦維持一個 **動態長度** 的大循環，時序邏輯如下：

| 階段名稱 | 拍數區間 | 聽覺行為 (Audio) | 視覺行為 (Visual) |
| :--- | :--- | :--- | :--- |
| **準備期 (Prep)** | 前 4 拍 | 第 1 拍播放**該和弦的真實木吉他刷扣伴奏**（微秒錯開之物理合成音），同時持續響起標準 Clicks 節拍。 | 指板上**僅常駐顯示該 Key 的灰色正方形定位點**，和弦根音與其餘音皆不顯示。強迫大腦用意念導航，不可偷看圖形。 |
| **訓練期 (Train)**| $N$ 拍 | 每 1 拍播放一個精準音階單音。彈奏完 $N$ 顆音後**立即無縫切換**至預告期，不再補齊拍數。 | **預設模式 (大循環折返)**：在當前把位內尋找最低音的根音出發 $\rightarrow$ 向上攻頂至把位最高音 $\rightarrow$ 向下探底至把位最低音 $\rightarrow$ 向上回歸至原出發根音。<br>**自訂模式**：循環播放使用者自訂級數音高。<br>發音音符在指板上實施**色彩美學閃爍**，並顯示**音程數字**。 |
| **預告期 (Predict)**| 最後 4 拍 | 單音演奏停止。節拍器自動切換為**有氧英語人聲播報："One, Two, Three, Four"**。 | 當前和弦維持置中且固定尺寸，其右側顯示「**→ 下一個和弦**」，下一個和弦以**與 BPM 同步的高頻黑/紅雙色閃爍**預告；**指板的音階把位虛線長方形提前切換到下一個目標和弦的 CAGED 把位**，先行導航下一個要找音的區間。 |

> **🚦 導入預告期 (Intro Predict)**：整段訓練最開始，系統會在第一個和弦的準備期 (Prep) 之前，先插入一段獨立的 **4 拍導入預告期**。此期間僅播放節拍器高音 Click 與有氧英語人聲倒數（"One, Two, Three, Four"），並讓第一個和弦於畫面正中央以黑/紅雙色閃爍，但**不刷扣伴奏、也不顯示「→ 下一個和弦」**，作為正式進入循環前的起跑導引。此導入期不會被計入「一輪和弦進行」，因此不會誤觸發 CAGED 把位推進。

---

## 🧠 4. 核心樂理技術：調式自動映射機制

為確保使用者在變換和弦時不需要痛苦地手動計算升降音（例如：什麼時候 6 要變成 ♭6），後台引擎自動啟動**順階調式映射（Diatonic Mode Mapping）**。

常駐顯示的灰色正方形為該 Key 的安全母音定位點；實際發聲的音階音只在播放到時才依音程色彩亮起。當自訂輸入 `L5, L6, 1, 2, L7, 1` 時，系統根據當前和弦級數自動對齊其專屬 Mode 暫存器：

* **I 級和弦 (以 C 為例)** $\rightarrow$ 對齊 **Ionian** ($1, 2, 3, 4, 5, 6, 7$)
  * 實體亮音：`G -> A -> C -> D -> B -> C` (全數吻合灰色點)
* **ii 級和弦 (以 Dm 為例)** $\rightarrow$ 對齊 **Dorian** ($1, 2, \flat3, 4, 5, 6, \flat7$)
  * 自動辨識 $7$ 為 $\flat7$，實體亮音：`A -> B -> D -> E -> C -> D` (全數吻合灰色點)
* **vi 級和弦 (以 Am 為例)** $\rightarrow$ 對齊 **Aeolian** ($1, 2, \flat3, 4, 5, \flat6, \flat7$)
  * 自動辨識 $6, 7$ 為 $\flat6, \flat7$，實體亮音：`E -> F -> A -> B -> G -> A` (全數吻合灰色點)

透過此演算法，使用者僅需在初始設定輸入一組心儀的通用相對旋律線，即可在和弦變換時，享受到完全不跑調且極具該級數調式色彩（Mode Color）的高階洗腦特訓。